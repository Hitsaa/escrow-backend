package com.blockchain.escrow.service;

import com.blockchain.escrow.dto.DeveloperProjectDto;

public interface DeveloperProjectService {
    DeveloperProjectDto proposeDeveloperProject(DeveloperProjectDto DeveloperProjectDto);
}
